/** Matches any run of characters in a glob pattern. */
export const GLOB_WILDCARD = '*';

/** Matches any run of characters in a CDP `Fetch.RequestPattern.urlPattern` (`?` matches one, `\` escapes). */
export const CDP_WILDCARD = '*';
