// Keeps the `./TreeLabel` import working now that its range helpers have their own files.
export { findMatches } from './findMatches';
export { TreeLabel, type TreeLabelProps } from './TreeLabel';
export type { TextRange } from './types';
