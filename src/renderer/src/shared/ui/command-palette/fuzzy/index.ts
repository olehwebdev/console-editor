// Keeps the `./fuzzy` import working now that each helper has its own file.
export { fuzzyMatch } from './fuzzyMatch';
export { normalizeQuery } from './normalizeQuery';
export type { FuzzyMatch } from './types';
