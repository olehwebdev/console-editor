// Keeps the `./PanelResizer` import working now that its key handling has its own file.
export { PanelResizer, type PanelResizerProps } from './PanelResizer';
export type { PanelResizerOrientation } from './types';
