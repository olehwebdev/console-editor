// Keeps the `./PanelResizer` import working now that its drag, keys and parts have their own files.
export { PanelResizer } from './PanelResizer';
export type { PanelResizerOrientation, PanelResizerProps } from './types';
