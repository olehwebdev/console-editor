// Adapted from beUI (https://beui.dev), MIT License, © 2026 Saurabh Chauhan.
import { createContext } from 'react';

/** Whether the tree below is inside a <HoverHighlight> that draws the hover pill. */
export const InHoverHighlight = createContext(false);
