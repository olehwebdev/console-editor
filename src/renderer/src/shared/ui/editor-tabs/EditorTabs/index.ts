// Keeps the `./EditorTabs` import working now that the tab and its helpers have their own files.
export { EditorTabs } from './EditorTabs';
export type { EditorTabItem, EditorTabsProps, EditorTabTone } from './types';
