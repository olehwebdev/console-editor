/** The URI authority of read-only models (original sources): the editor won't let them be typed into. */
export const READ_ONLY_URI_AUTHORITY = 'source';

/** What typing into a read-only original says (it names no key, so it is right on every platform). */
export const READ_ONLY_MESSAGE = { value: 'Original sources are read-only. Use Go to bundle code to change what the page runs.' };
