/** `MouseEvent.button` values the UI tells apart. */
export const MOUSE_BUTTON = {
  /** The main button, usually the left. */
  primary: 0,
  /** The wheel or middle button. */
  middle: 1,
  /** The secondary button, usually the right (a context menu). */
  secondary: 2,
} as const;
