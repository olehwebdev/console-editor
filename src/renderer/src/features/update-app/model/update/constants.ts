export const WHATS_NEW_TAB = 'page:whats-new';

/** One toast for the update, replaced as it moves along. */
export const UPDATE_TOAST_ID = 'app-update';

/** A disk image: the main process opens it again (mounted, in Finder), where it shows other downloads in their folder. */
export const DMG_EXTENSION = '.dmg';
