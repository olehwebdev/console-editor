/** Where a workspace's prompt history is kept: this prefix and the workspace id. */
export const HISTORY_STORAGE_PREFIX = 'console-editor:console-history:';

/** Lines of code the prompt remembers per workspace. */
export const MAX_HISTORY = 100;
