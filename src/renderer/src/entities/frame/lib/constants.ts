/** The top page's key: it stays the same whichever page the top frame shows. */
export const TOP_FRAME_KEY = 'top';

/** Keys of iframes with no web address, by their `name`, else their id: never a URL, nor each other. */
export const NAME_KEY_PREFIX = 'name:';
export const ID_KEY_PREFIX = 'id:';
