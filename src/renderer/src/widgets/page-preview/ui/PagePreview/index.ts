// Keeps the slice's `./ui/PagePreview` import working now that its hooks and parts have their own files.
export { PagePreview } from './PagePreview';
export type { PagePreviewProps } from './types';
