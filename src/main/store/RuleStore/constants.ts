/** The rules of every workspace, next to overrides.json. */
export const INDEX_FILE = 'rules.json';

/** Written, never branched on: older builds never open this file. */
export const INDEX_VERSION = 1;

/** Where an unreadable rules.json is kept: `rules.json.broken`. */
export const BROKEN_SUFFIX = '.broken';
